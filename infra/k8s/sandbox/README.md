# sandbox (reserved)

This directory is reserved for a future Kubernetes-based sandbox deployment target.

The current sandbox environment is implemented through:

- `infra/docker/docker-compose.sandbox.yml`
