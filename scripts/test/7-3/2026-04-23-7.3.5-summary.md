# Batch 7.3.5 DB-Fix Rerun Summary

- Stage: `7.3.5`（按 7.3.4 结论做最小 DB 路径修复后复测）
- Date: `2026-04-23` (EDT)
- Profile: `Profile A`
- Fixed product: `PRODUCT_ID=2`
- Load profile: `VUS=500`, `WARMUP=20s`, `STEADY=40s`, `COOLDOWN=10s`

## 1) Applied Code Change (order-service)

File: `services/order-service/src/main/java/com/example/orders/service/OrderService.java`

1. `createOrder(...)` 去掉方法级 `@Transactional`
- 目标: 缩短事务生命周期，避免在远程调用 `product-service` 时占用 DB 连接。

2. 新增 `DataIntegrityViolationException` 并发兜底
- 目标: 并发重复请求号冲突时，回收已预占库存，并返回既有订单（`user_id + request_no`）。

## 2) Execution Notes

1. `dbfix-s1`
- 在 MySQL 慢日志 TABLE 采集开启状态下执行（用于热点定位）。

2. `dbfix-s2`
- 关闭慢日志额外开销后执行，作为本阶段主判定样本。

Gateway limiter handling:
- before: `20/60`
- temp during test: `60000/60`
- restored: `20/60`（见 `2026-04-23-7.3.5-gateway-rate-limit-restored.txt`）

## 3) Key Results (7.3.4 tuned-s2 vs 7.3.5 dbfix-s2)

- `http_req_failed`: `0 -> 78.48%`
- `b72_unexpected_5xx_rate`: `0 -> 75.42%`
- `http_req_duration p95`: `10413.48ms -> 15063.43ms`
- `b72_create_latency p95`: `11059.87ms -> 15072.44ms`
- `http_reqs`: `11593 -> 3448`
- `iterations`: `3161 -> 2879`

结论: 本轮改动未改善端到端稳定性，反而出现灾难性退化。

## 4) Root Cause Evidence

核心证据来自 `product-service`（`2026-04-23-7.3.5-product-service-logs-s2.log`）:

- 重复出现:
  - `QueuePool limit of size 5 overflow 10 reached, connection timed out, timeout 30.00`
- 统计命中: `778` 次

配套证据:

- `gateway` 侧 `httpx.ReadTimeout` 统计命中: `1796` 次
- 说明链路主要卡在 `order-service -> product-service` 上游调用等待，而非 gateway 前端层本身。

## 5) Stage Decision

- ✅ 根因定位成功: 明确瓶颈在 `product-service` SQLAlchemy 连接池（默认 `5 + 10`）耗尽。
- ⚠️ 本阶段性能结果不通过: 高失败率、高 5xx、高 p95 超时。
- ✅ 进入下一步: 先做 `product-service` 连接池参数化与扩容，再复测验证（对应 `7.3.6`）。
