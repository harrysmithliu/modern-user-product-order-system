# Batch 7.3.6 Product-Pool Tuning Rerun Summary

- Stage: `7.3.6`（针对 7.3.5 根因: product-service pool 耗尽）
- Date: `2026-04-23` (EDT)
- Profile: `Profile A`
- Fixed product: `PRODUCT_ID=2`
- Load profile: `VUS=500`, `WARMUP=20s`, `STEADY=40s`, `COOLDOWN=10s`

## 1) Applied Tuning Changes

### 1.1 Product-service connection pool parameterization

File: `services/product-service/app/core/config.py`

新增配置项:
- `sqlalchemy_pool_size=30`
- `sqlalchemy_max_overflow=60`
- `sqlalchemy_pool_timeout_seconds=10`
- `sqlalchemy_pool_recycle_seconds=1800`

### 1.2 Product-service engine pool settings effective

File: `services/product-service/app/db/session.py`

`create_engine(...)` 显式注入:
- `pool_size`
- `max_overflow`
- `pool_timeout`
- `pool_recycle`
- `pool_pre_ping`

### 1.3 Sandbox compose and env docs

- `infra/docker/docker-compose.sandbox.yml`
  - `PRODUCT_SERVICE_SQLALCHEMY_POOL_SIZE`
  - `PRODUCT_SERVICE_SQLALCHEMY_MAX_OVERFLOW`
  - `PRODUCT_SERVICE_SQLALCHEMY_POOL_TIMEOUT_SECONDS`
  - `PRODUCT_SERVICE_SQLALCHEMY_POOL_RECYCLE_SECONDS`
- `services/product-service/.env.example`
- `services/product-service/README.md`

已验证容器生效:
- `PRODUCT_SERVICE_SQLALCHEMY_POOL_SIZE=30`
- `PRODUCT_SERVICE_SQLALCHEMY_MAX_OVERFLOW=60`
- `PRODUCT_SERVICE_SQLALCHEMY_POOL_TIMEOUT_SECONDS=10`
- `PRODUCT_SERVICE_SQLALCHEMY_POOL_RECYCLE_SECONDS=1800`

## 2) Key Results (7.3.5-s2 vs 7.3.6-s1)

### Stability and error recovery

- `http_req_failed`: `78.48% -> 3.58%`
- `b72_unexpected_5xx_rate`: `75.42% -> 3.11%`
- `b72_http_5xx_total`: `2599 -> 183`
- `checks_success_rate`: `7.53% -> 88.81%`

### Throughput and latency

- `http_reqs`: `3448 -> 5891`（提升）
- `iterations`: `2879 -> 1750`（下降）
- `http_req_duration p95`: `15063ms -> 15789ms`（仍超阈值）
- `b72_create_latency p95`: `15072ms -> 15582ms`（仍超阈值）
- `b72_pay_latency p95`: `53ms -> 15533ms`（长尾明显）

## 3) Bottleneck Evidence After Tuning

1. Product-service pool timeout still exists, but reduced:
- 7.3.5: `QueuePool size 5 + overflow 10` timeout `778` 次
- 7.3.6: `QueuePool size 30 + overflow 60` timeout `15` 次

2. Gateway upstream timeout reduced but not zero:
- `httpx.ReadTimeout` in gateway logs: `1796 -> 145`

3. Order-service pool snapshot remains healthy:
- `hikaricp_connections_pending` post snapshot: `0`

结论: 本轮确实缓解了 product-service 连接池瓶颈，但系统仍有显著 15s 级长尾，尤其在 create/pay 链路。

## 4) Data Integrity Checks

- duplicate `(user_id, request_no)`: none
- due shipping count: `0`
- product stock (`id=2`): `45112 -> 44200`（`-912`）

## 5) Stage Decision

- ✅ 有效修复: 从“灾难性失败”恢复到“低错误率可运行”。
- ⚠️ 尚未达标: 核心 p95 指标仍远高于阈值（create/pay/http_req_duration）。
- ✅ 建议进入下一步 `7.3.7`:
  1. 在不放大 5xx 的前提下，继续削减 15s 长尾（优先处理 create/pay）。
  2. 以 product/order 内部链路为对象做“快失败 + 并发整形”验证，避免请求堆积到网关上游超时窗口。
