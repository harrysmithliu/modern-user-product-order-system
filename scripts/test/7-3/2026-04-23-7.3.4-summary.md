# Batch 7.3.4 Tuned Rerun Summary

- Stage: `7.3.4`（按 7.3.3 结论实施调优后复测）
- Date: `2026-04-23` (EDT)
- Branch/HEAD: `sandbox-batch7-baseline` / `e7ff3cd`（开始本轮时）
- Profile: `Profile A`
- Fixed product: `PRODUCT_ID=2`
- Load profile: `VUS=500`, `WARMUP=20s`, `STEADY=40s`, `COOLDOWN=10s`

## 1) Applied Tuning Changes

Code/config changes applied before rerun:

1. Gateway upstream client reuse
- file: `gateway/app/core/proxy.py`
- change: request-scope `httpx.AsyncClient` -> process lifecycle singleton client

2. Gateway upstream pool settings
- file: `gateway/app/core/config.py`
- new settings:
  - `upstream_max_connections=2048`
  - `upstream_max_keepalive_connections=512`
  - `upstream_keepalive_expiry_seconds=30`

3. Gateway lifespan startup/shutdown client management
- file: `gateway/app/main.py`
- change: create/close shared `httpx.AsyncClient` in FastAPI lifespan

4. Gateway multi-worker runtime
- file: `gateway/Dockerfile`
- change: uvicorn supports env-based workers, default `GATEWAY_UVICORN_WORKERS=2`

5. Order-service Hikari + JVM baseline in sandbox compose
- file: `infra/docker/docker-compose.sandbox.yml`
- key env:
  - `SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE=20`
  - `SPRING_DATASOURCE_HIKARI_MINIMUM_IDLE=10`
  - `SPRING_DATASOURCE_HIKARI_CONNECTION_TIMEOUT=3000`
  - `JAVA_TOOL_OPTIONS=-Xms1g -Xmx1g ...`

Verified effective env snapshots:

- `2026-04-23-7.3.4-gateway-rate-limit-before.txt`
- `2026-04-23-7.3.4-order-env-before.txt`

## 2) Rerun Execution Notes

Two tuned runs were executed:

1. `tuned-s1`
- summary exists: `2026-04-23-7.3.4-tuned-s1-summary.json`
- note: `.log` path was incorrect for first run, so it was re-run for complete log evidence

2. `tuned-s2` (authoritative for this report)
- log: `2026-04-23-7.3.4-tuned-s2.log`
- summary: `2026-04-23-7.3.4-tuned-s2-summary.json`

Gateway limiter handling:

- before: `20/60`
- temp during test: `60000/60`
- restored: `20/60`

Artifacts:

- `2026-04-23-7.3.4-gateway-rate-limit-before.txt`
- `2026-04-23-7.3.4-gateway-rate-limit-temp.txt`
- `2026-04-23-7.3.4-gateway-rate-limit-restored.txt`

## 3) Key Results (7.3.2 vs 7.3.4 tuned-s2)

### Throughput and error rate

- `http_reqs`: `9426 -> 11593` (`+2167`)
- `iterations`: `2570 -> 3161` (`+591`)
- `http_req_failed`: `0 -> 0` (s2)

### Latency

- `http_req_duration p95`: `5896.94ms -> 10413.48ms` (worse)
- `b72_create_latency p95`: `25608.81ms -> 11059.87ms` (better but still far above threshold)
- `b72_pay_latency p95`: `4343.27ms -> 10282.49ms` (worse)
- `http_req_duration median`: `2546.43ms -> 988.01ms` (median improved, tail worsened)

### Runtime / bottleneck shift signals

- 7.3.2 mid snapshot:
  - gateway CPU `101.88%`
  - order-service CPU `41.14%`
- 7.3.4 mid snapshot:
  - gateway CPU `4.80%`
  - order-service CPU `81.09%`

Interpretation: gateway bottleneck was relieved; pressure shifted downstream to order-service/DB path.

### JVM and pool observations

- GC overhead remained low (`~5.23e-4`)
- Old Gen usage remained controlled in sampled points
- live threads rose to `217`
- Hikari max pool is confirmed `20`
- latest s2 log window: `WARN=0`, `ERROR=0`

## 4) Data Integrity Checks

- duplicate `(user_id, request_no)`: none
- due shipping count: `0`
- product stock delta (`id=2`): `48733 -> 45858` (`-2875`)

## 5) Stage Decision

Current decision for `7.3.4`:

- ✅ 调优改动已成功生效并完成复测。
- ⚠️ 目标阈值仍未达标（尤其总体/支付 p95 长尾）。
- ✅ 关键价值：已确认瓶颈重心从 gateway 前移到 order-service/DB 侧，为下一步调优提供明确方向。

Recommended next tuning focus:

1. DB capacity lane
- further raise pool and DB concurrency budget with controlled A/B (`20 -> 30/40`)
- validate MySQL execution/lock/index hotspots with slow query evidence

2. Order-service concurrency control
- cap or reshape request concurrency per critical endpoint to reduce tail queueing
- inspect thread model and blocking sections around order create/pay transaction path

3. Keep gateway tuning as-is
- current gateway changes improved throughput and reduced gateway CPU hotspot
