# Batch 7.3.7 Concurrency Shaping Rerun Summary

- Stage: `7.3.7`（按 7.3.6 结论继续收敛长尾）
- Date: `2026-04-24` (EDT)
- Profile: `Profile A`
- Fixed product: `PRODUCT_ID=2`
- Load profile: `VUS=500`, `WARMUP=20s`, `STEADY=40s`, `COOLDOWN=10s`

## 1) Applied Changes

### 1.1 order-service: product-call shaping via existing external guard

File: `services/order-service/src/main/java/com/example/orders/service/OrderService.java`

- `createOrder`:
  - `product get/reserve/release` 接入 `ExternalCallGuard`
  - 回滚释放改为 `tryReleaseProduct(...)`，避免释放失败覆盖主异常
- `cancelOrder` / `reject`:
  - `releaseStock` 接入 guard

### 1.2 sandbox runtime tuning for guard

File: `infra/docker/docker-compose.sandbox.yml`

- `APP_WORKFLOW_EXTERNAL_SEMAPHORE_PERMITS=120`
- `APP_WORKFLOW_SEMAPHORE_ACQUIRE_TIMEOUT_MS=100`

### 1.3 gateway: upstream error normalization

File: `gateway/app/core/proxy.py`

- 捕获 `httpx.TimeoutException` -> `504` JSON
- 捕获 `httpx.RequestError` -> `502` JSON

## 2) Runtime Note

本轮开始时发现 `order-service` 重启循环，根因是运行时依赖容器未启动（`mysql/product-service/prometheus`）。已先恢复：

- `docker compose -f infra/docker/docker-compose.sandbox.yml up -d`
- `docker compose -f infra/docker/docker-compose.monitoring.yml up -d`

之后执行正式复测。

## 3) Key Results (7.3.6-s1 vs 7.3.7-s1)

### Stability and error rate

- `http_req_failed`: `3.58% -> 0.00%`
- `b72_unexpected_5xx_rate`: `3.11% -> 0.00%`
- `b72_http_5xx_total`: `183 -> 0`
- `checks_success_rate`: `88.81% -> 100.00%`

### Throughput and latency

- `http_reqs`: `5891 -> 15088`
- `iterations`: `1750 -> 4114`
- `http_req_duration median`: `2470ms -> 1138ms`
- `http_req_duration p95`: `15789ms -> 6190ms`
- `b72_create_latency p95`: `15582ms -> 6447ms`
- `b72_pay_latency p95`: `15533ms -> 5989ms`

结论: 7.3.7 相比 7.3.6 已显著收敛并恢复稳定，但 p95 仍未达到目标阈值（`create<2000ms`, `pay<2500ms`, `http_req_duration<3000ms`）。

## 4) Evidence Highlights

- gateway logs:
  - `httpx.ReadTimeout` 命中 `0`
- product-service logs:
  - `QueuePool size 30 overflow 60 timeout` 命中 `0`
- order-service actuator:
  - `hikaricp_connections_pending` pre/post 均为 `0`

## 5) Data Checks

- duplicate `(user_id, request_no)`: none
- product stock (`id=2`): `44200 -> 42146`（`-2054`）
- due shipping count: `2017`
  - 注: 当前日期已是 `2026-04-24`，与前一日基线（`2026-04-23`）相比，历史订单进入“到期”窗口，不能直接按单轮压测回归解读。

## 6) Stage Decision

- ✅ 7.3.7 目标（继续压缩 15s 级长尾）已取得明显进展。
- ⚠️ 仍未满足最终阈值，建议继续 `7.3.8` 做尾延迟专项（create/pay 慢查询与热点行竞争拆分）。
