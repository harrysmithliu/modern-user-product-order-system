# Batch 7.3.9.1 Rollback Baseline + DB Hotspot Summary

- Stage: `7.3.9.1`（按 7.3.8 结论执行回退，并开启 DB 热点取证）
- Date: `2026-04-24` (EDT)
- Profile: `Profile A`
- Fixed product: `PRODUCT_ID=2`
- Load profile: `VUS=500`, `WARMUP=20s`, `STEADY=40s`, `COOLDOWN=10s`

## 1) Applied Change

File: `services/order-service/src/main/java/com/example/orders/service/ExternalCallGuard.java`

- 已回退 `callSync(...)` 到 7.3.7 路径：
  - `CompletableFuture.supplyAsync(supplier, workflowExecutor).join()`
  - 保留 `CompletionException` 解包逻辑

## 2) Key Results (7.3.7-s1 vs 7.3.9.1-s1)

### Stability

- `http_req_failed`: `0.00% -> 0.00%`
- `b72_unexpected_5xx_rate`: `0.00% -> 0.00%`
- `checks_success_rate`: `100.00% -> 100.00%`

### Throughput and latency

- `http_reqs`: `15088 -> 10820`
- `iterations`: `4114 -> 2950`
- `http_req_duration median`: `1138ms -> 1601ms`
- `http_req_duration p95`: `6190ms -> 8823ms`
- `b72_create_latency p95`: `6447ms -> 9504ms`
- `b72_pay_latency p95`: `5989ms -> 9207ms`

### Threshold status (7.3.9.1)

- ✅ `http_req_failed rate<0.30` (`0.00%`)
- ✅ `b72_unexpected_5xx_rate rate<0.05` (`0.00%`)
- ❌ `http_req_duration p95<3000` (`8823ms`)
- ❌ `b72_create_latency p95<2000` (`9504ms`)
- ❌ `b72_pay_latency p95<2500` (`9207ms`)

结论: 仅回退 7.3.8 的改动后，稳定性保持，但性能没有回到 7.3.7 水平。

## 3) DB Hotspot Evidence

MySQL digest top SQL (`performance_schema.events_statements_summary_by_digest`):

1. `h_product_db UPDATE t_product SET stock = stock - ? ... WHERE id=? AND stock>=?`
   - `COUNT_STAR=2950`
   - `SUM_WAIT_S=81.334`
   - `AVG_WAIT_MS=27.571`
   - `LOCK_S=79.930`
2. `h_product_db UPDATE t_product SET stock = stock + ? ... WHERE id=?`
   - `COUNT_STAR=1475`
   - `SUM_WAIT_S=28.471`
   - `LOCK_S=27.822`

`sys.innodb_lock_waits` 抽样时无活跃行（0 行），但 digest 的高 `LOCK_S` 说明热点主要集中在同一商品库存行更新竞争。

## 4) Data Checks

- duplicate `(user_id, request_no)`: none
- product stock (`id=2`): `40680 -> 39205`（`-1475`）
- due shipping count: `1617`

## 5) Stage Decision and Next Move

- ✅ 已完成“先提交 7.3.8，再回退到 7.3.7 代码路径并复测”的动作。
- ⚠️ 当前主要瓶颈已定位到库存热点行更新（`t_product.id=2`）的锁时间累积。
- 下一步建议（7.3.9.2）:
  1. 将压测流量改为多商品分片（避免单行热点），验证尾延迟是否随热点稀释而下降。
  2. 针对库存更新 SQL 增加更细粒度观测（按 product_id 分组的耗时与冲突计数）。
