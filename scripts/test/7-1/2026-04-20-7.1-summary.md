# Batch 7.1 Baseline and Observability Freeze

- Bundle path: `scripts/test/7-1/`
- Date: `2026-04-20`
- Timezone: `America/Toronto`
- Branch: `sandbox-batch7-baseline`
- Commit SHA: `b178c61`
- Target gateway: `http://127.0.0.1:8000`
- Scope profile: `Profile A (external simulated platforms disabled)`
- Step type: `No business-code change`

## 1) Entry Gate Result

SOP mandatory checks all passed:

- gateway readiness: `200` (`2026-04-20-7.1-gateway-ready.json`)
- gateway liveness: `200` (`2026-04-20-7.1-gateway-live.json`)
- order-service actuator health: `200` (`2026-04-20-7.1-order-health.json`)
- Prometheus ready: `200` (`2026-04-20-7.1-prometheus-ready.txt`)
- Grafana health: `200` (`2026-04-20-7.1-grafana-health.json`)
- schema sanity check (`h_user_db.t_user.login_enabled`) exists (`2026-04-20-7.1-schema-check.txt`)

Container status snapshots are stored in:

- `2026-04-20-7.1-sandbox-ps.txt`
- `2026-04-20-7.1-monitoring-ps.txt`

## 2) Frozen Runtime Settings (order-service)

Source of truth:

- runtime env from container inspect
- `services/order-service/src/main/resources/application.yml` default fallback

Frozen values:

- executor core pool size: `16`
- executor max pool size: `64`
- executor queue capacity: `2000`
- external call semaphore permits: `200`
- semaphore acquire timeout ms: `200`
- coupon issue retry fixed delay ms: `3000`
- coupon issue retry delay seconds: `30`
- coupon issue max retries: `8`
- auto-complete main cron: `0 15 9 * * MON-FRI`
- auto-complete zone: `America/Toronto`
- auto-complete compensation fixed delay ms: `3600000`
- auto-complete batch size: `100`
- payment platform enabled: `false` (runtime default, no env override found)
- coupon platform enabled: `false` (runtime default, no env override found)

## 3) Small-Sample Order Flow Validation

Execution artifact:

- `2026-04-20-7.1-metrics.json`
- `2026-04-20-7.1-metrics.csv`
- `2026-04-20-7.1-request-log.jsonl`

Run method:

- login user/admin once
- get product list and choose first product id
- run 10 sample chains through gateway:
  - `POST /api/orders`
  - `POST /api/orders/{id}/pay`
  - half requests -> `POST /api/admin/orders/{id}/approve`
  - half requests -> `POST /api/admin/orders/{id}/reject`

Sample output:

- created order ids: `57..66`
- failures: `0`

Latency and error summary:

| Endpoint | Samples | Avg (ms) | p95 (ms) | Error Rate |
|---|---:|---:|---:|---:|
| `POST /api/orders` | 10 | 47.274 | 251.513 | 0.0 |
| `POST /api/orders/{id}/pay` | 10 | 18.355 | 43.445 | 0.0 |
| `POST /api/admin/orders/{id}/approve` | 5 | 18.742 | 28.268 | 0.0 |
| `POST /api/admin/orders/{id}/reject` | 5 | 23.221 | 32.498 | 0.0 |

Order transition counts:

- `PAYING -> PAID_PENDING_APPROVAL`: `10`
- `PAID_PENDING_APPROVAL -> SHIPPING`: `5`
- `PAID_PENDING_APPROVAL -> REJECTED`: `5`

## 4) Status Distribution Snapshot

Pre-sample status counts:

- `2026-04-20-7.1-order-status-counts-pre.tsv`

Post-sample status counts:

- `2026-04-20-7.1-order-status-counts-post.tsv`

Observed deltas:

- status `2` (`REJECTED`): `+5` (from `11` to `16`)
- status `6` (`SHIPPING`): `+5` (from `1` to `6`)
- others unchanged in this run window

## 5) Observability Evidence

Prometheus/Grafana evidence:

- target snapshot: `2026-04-20-7.1-prometheus-targets.json`
- order-service actuator metrics snapshot: `2026-04-20-7.1-order-actuator-prometheus.txt`
- extracted JVM/executor/http key lines: `2026-04-20-7.1-jvm.txt`

Key observations:

- Prometheus `up` targets include sandbox `8000/8001/8002/8080` and self `9090`
- Prometheus `down` targets are dev endpoints `8010/8011/8012/8081` (expected in current local run)
- order-service counters captured requested endpoints with success status:
  - `/orders`: count `10`
  - `/orders/{orderId}/pay`: count `10`
  - `/admin/orders/{orderId}/approve`: count `5`
  - `/admin/orders/{orderId}/reject`: count `5`

## 6) Tools Understanding in This Project

- `k6`:
  - load-test scripts are in `scripts/load/`
  - current scripts include `k6-products.js` and `k6-orders.js`
  - local `k6` binary is not installed in this workspace; Docker run path is already documented
- `Prometheus`:
  - bootstrap via `infra/docker/docker-compose.monitoring.yml`
  - scrapes sandbox and dev targets; sandbox targets are used for Batch 7 validation
- `Grafana`:
  - same compose stack as Prometheus
  - auto-provisions datasource and dashboard from `infra/monitoring/grafana/provisioning`

## 7) 7.1 Exit-Criteria Check

- baseline data can be reproduced from this result bundle: `PASS`
- no business/schema code changes introduced for this step: `PASS`
- observability evidence and command artifacts stored under `results/`: `PASS`
